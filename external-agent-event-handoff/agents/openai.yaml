interface:
  display_name: "External Agent Event Handoff"
  short_description: "Single handoffs or Codex-reviewed hardness loops"
  default_prompt: "Use $external-agent-event-handoff with an explicit thread ID in single or loop mode."
